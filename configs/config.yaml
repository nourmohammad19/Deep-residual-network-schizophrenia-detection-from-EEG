# configs/config.yaml
project_name: "schizo-resnet-v1"
data:
  image_size: 224
  batch_size: 32
  train_test_ratio: 0.7

model:
  name: "resnet50"
  num_classes: 2  # Schizophrenia vs Control

training:
  epochs: 50
  learning_rate: 0.0001
  save_dir: "./outputs/weights/"